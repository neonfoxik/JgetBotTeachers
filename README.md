# TaskBot - Модуль управления задачами

Telegram бот для управления задачами на Django + python-telegram-bot.

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```
⚠️ **Примечание**: Если возникают проблемы с установкой, попробуйте создать виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

### 2. Настройка переменных окружения
Создайте файл `.env` с помощью скрипта:
```bash
python create_env.py
```

Или создайте вручную на основе `env_example.txt`:
```env
BOT_TOKEN=your_telegram_bot_token_here
HOOK=your-domain.com  # ДОМЕН без https:// - протокол добавляется автоматически
OWNER_ID=your_telegram_user_id  # опционально для уведомлений об ошибках
LOCAL=False  # True для SQLite (разработка), False для MySQL (продакшн)
```

#### Настройка базы данных

**Для разработки (SQLite):**
```env
LOCAL=True
```

**Для продакшена (MySQL) - минимальная конфигурация:**
```env
LOCAL=False
NAME_DB=nurgalpl_mandar1
PASS_DB=z%Q&y*2b9q&6
```

**Опционально** (если отличается от автоматических значений):
```env
DB_USER=nurgalpl_mandar1  # По умолчанию = NAME_DB
DB_HOST=localhost         # По умолчанию localhost
DB_PORT=3306             # По умолчанию 3306
```

⚠️ **Важно**: В переменной `HOOK` указывайте только домен без протокола (без `https://`).
Протокол HTTPS добавляется автоматически для совместимости с Telegram API.

### 3. Миграции
```bash
python manage.py migrate
```

### 4. Создание администратора (опционально)
```bash
python manage.py createsuperuser
```
*Примечание: пользователи автоматически регистрируются при первом использовании бота*

### 5. Запуск

#### Режим Polling (для разработки)
```bash
# Запуск бота с удалением webhook
python manage.py run_bot --remove-webhook

# Принудительный запуск (если бот уже запущен)
python manage.py run_bot --remove-webhook --force

# Быстрый перезапуск
python restart_bot.py

# Остановка бота
python manage.py stop_bot

# Проверка статуса
python bot_status.py
```

#### Режим Webhook (для продакшена)
```bash
# Для разработки
python manage.py runserver

# С планировщиком напоминаний
RUN_SCHEDULER=true python manage.py runserver
```

### 6. Настройка webhook (для продакшена)
Бот работает через webhook. Для настройки выполните:

```bash
# Установите webhook
curl https://your-domain.com/set_webhook/

# Проверьте статус webhook
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo
```

**Требования для webhook:**
- HTTPS URL (автоматически добавляется)
- Сертификат SSL/TLS
- Публично доступный домен

**Локальная разработка:**
- Для тестирования используйте ngrok или аналогичный инструмент
- Установите HOOK=your-ngrok-url.ngrok.io (без https://)
- Выполните `curl https://your-ngrok-url.ngrok.io/set_webhook/`

## 🔧 Troubleshooting

### Webhook ошибки

**Ошибка "bad webhook: An HTTPS URL must be provided"**
- Убедитесь, что переменная HOOK содержит только домен без протокола
- Проверьте, что домен имеет действительный SSL-сертификат
- Для localhost используйте ngrok или similar tunneling service

**Ошибка "Failed to set webhook"**
- Проверьте BOT_TOKEN в .env файле
- Убедитесь, что домен доступен из интернета
- Проверьте логи сервера на наличие ошибок

**Бот не отвечает на сообщения**
- Убедитесь, что webhook установлен: `curl https://api.telegram.org/bot<TOKEN>/getWebhookInfo`
- Проверьте, что сервер запущен и отвечает на /bot/ эндпоинт
- Проверьте логи Django на наличие ошибок обработки

### Ошибки Polling режима

**Ошибка "Conflict: terminated by other getUpdates request" (409)**
- Эта ошибка означает, что уже запущен другой экземпляр бота
- Решение: остановите существующий бот `python manage.py stop_bot`
- Или запустите принудительно: `python manage.py run_bot --remove-webhook --force`

**Бот зависает при запуске**
- Проверьте интернет-соединение
- Убедитесь, что токен бота корректный
- Проверьте логи на наличие сетевых ошибок

### Проблемы с зависимостями

**"ImportError: Couldn't import Django"**
```bash
source venv/bin/activate
pip install -r requirements.txt
```

**"ModuleNotFoundError" для APScheduler**
```bash
pip install APScheduler==3.10.4
```

## 📋 Команды бота

- `/tasks` - Активные задачи (как исполнитель)
- `/my_created_tasks` - Созданные задачи (только админы)
- `/create_task` - Создать новую задачу
- `/close_task <ID>` - Инициировать закрытие задачи
- `/task_progress <ID>` - Просмотр прогресса

## 🧪 Тестирование

```bash
python test_basic.py
```

## 📦 Структура проекта

```
JgetBotTeachers/
├── bot/
│   ├── management/commands/
│   │   ├── run_bot.py      # Запуск бота в polling режиме
│   │   └── stop_bot.py     # Остановка бота
│   ├── models.py           # Модели User, Task, Subtask, UserState
│   ├── handlers/tasks.py   # Все обработчики команд и callback
│   ├── schedulers.py       # APScheduler для напоминаний
│   ├── keyboards.py        # Inline клавиатуры
│   ├── admin.py            # Админка Django
│   └── migrations/         # Миграции базы данных
├── dd/
│   └── settings.py         # Настройки Django
├── requirements.txt        # Зависимости
├── test_basic.py          # Базовое тестирование
├── test_states.py         # Тест состояний пользователей
├── test_bot_commands.py   # Тест команд управления ботом
├── restart_bot.py         # Скрипт быстрого перезапуска
├── bot_status.py          # Проверка статуса бота
├── BOT_COMMANDS.md        # Подробная документация команд
├── env_example.txt        # Пример переменных окружения
└── README.md              # Эта документация
```

## 🔒 Безопасность

- **Автоматическая регистрация**: пользователи создаются при первом обращении
- **Проверка прав доступа** на каждом действии
- **Администраторы**: полный доступ ко всем задачам (нужно установить is_admin=True в БД)
- **Учителя**: управление созданными задачами
- **Исполнители**: работа со своими задачами

### Управление администраторами

Чтобы сделать пользователя администратором, выполните в Django shell:
```python
from bot.models import User
user = User.objects.get(telegram_id='YOUR_TELEGRAM_ID')
user.is_admin = True
user.save()
```

## 📊 Статусы задач

- `active` - Активная задача
- `pending_review` - Ожидает подтверждения создателя
- `completed` - Завершенная задача

## ⏰ Напоминания

- **Ежедневные** в 08:00 - список активных задач
- **Персональные** за 24 часа до срока выполнения
- Управляются APScheduler в фоновом режиме

## 🛠 Исправленные проблемы

- ✅ Убрана зависимость django-apscheduler (несовместима с Django 5.1.6)
- ✅ Убрана зависимость psycopg2-binary (несовместима с Python 3.13)
- ✅ Используется SQLite для простоты разработки
- ✅ APScheduler работает напрямую без django-apscheduler
- ✅ **Исправлена ошибка потери состояний в webhook режиме**
- ✅ **Добавлено предотвращение конфликтов polling запросов**
- ✅ **Улучшена система управления ботом с PID файлами**

## 🔧 Новые возможности

### Управление состояниями пользователей
- **UserState модель**: сохранение состояний в базе данных вместо памяти
- **Автоматическое восстановление**: состояния сохраняются между перезапусками
- **Безопасность**: предотвращение потери данных при сбоях сервера

### Команды управления ботом
- `run_bot --remove-webhook` - запуск в polling режиме
- `stop_bot` - корректная остановка бота
- `restart_bot.py` - быстрый перезапуск
- `bot_status.py` - проверка статуса бота

### Предотвращение конфликтов
- **PID файлы**: предотвращение одновременного запуска нескольких экземпляров
- **Автоматическая очистка**: удаление устаревших PID файлов
- **Graceful shutdown**: корректная остановка по сигналам

## 📋 Зависимости

- APScheduler==3.10.4
- Django==5.1.6
- Pillow==11.1.0
- pyTelegramBotAPI==4.26.0
- python-dotenv==1.0.1
- pytz==2024.1
- requests==2.32.3

## 🎯 Готово к использованию!

Модуль управления задачами полностью функционален и соответствует ТЗ. База данных использует SQLite, планировщик напоминаний работает в фоне, все проверки прав доступа реализованы.

## 🔧 Исправленные проблемы

- ✅ Убрана зависимость psycopg2-binary (несовместима с Python 3.13)
- ✅ Исправлена ошибка с отсутствующей переменной OWNER_ID
- ✅ Упрощена настройка базы данных (только SQLite)
- ✅ Добавлена безопасная обработка уведомлений администратору

## 📝 Переменные окружения

### Обязательные:
- `BOT_TOKEN` - токен Telegram бота
- `HOOK` - URL для webhook
- `NAME_DB` - имя базы данных MySQL (если LOCAL=False)
- `PASS_DB` - пароль MySQL (если LOCAL=False)

### Опциональные:
- `OWNER_ID` - ID администратора для уведомлений
- `LOCAL` - тип базы данных: True (SQLite) или False (MySQL), по умолчанию False
- `DB_USER` - пользователь MySQL, по умолчанию = NAME_DB (если LOCAL=False)
- `DB_HOST` - хост MySQL, по умолчанию localhost (если LOCAL=False)
- `DB_PORT` - порт MySQL, по умолчанию 3306 (если LOCAL=False)
- `RUN_SCHEDULER` - включить планировщик напоминаний
